<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @project.name %>
</p>

<p>
  <strong>Startdate:</strong>
  <%= nice_date_form(@project.startDate) %>
</p>

<p>
  <strong>Enddate:</strong>
  <%= nice_date_form(@project.endDate) %>
</p>

<p>
  <strong>Group:</strong>
  <%= Group.find(@project.group_id).name %>
</p>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
